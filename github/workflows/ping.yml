name: Keep Supabase Warm

on:
  schedule:
    - cron: "*/10 * * * *" # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Make requests
        run: |
          curl -s https://fitness-ai-beryl-theta.vercel.app/api/plans/ping || true
          curl -s https://fitness-ai-beryl-theta.vercel.app/api/progress || true
